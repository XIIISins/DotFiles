#!/usr/local/bin/bash
#
#List content of random directories
#

random_ls ()  {
  for i in $(find . -maxdepth 1 -name "*pdb*"  | shuf -n4); do
    echo -e "\n\n$i\n"
    ls -ahl $i | tail -4
  done
}

random_ls
